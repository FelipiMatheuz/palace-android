<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/dark_gray"
        android:layout_margin="8dp">
    <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/dialog_password"
            android:textColor="@color/white"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:id="@+id/tv_view_room_password_label"
            android:layout_marginTop="16dp"
            android:layout_marginStart="16dp"/>

    <EditText
            android:id="@+id/et_view_room_password"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            app:layout_constraintTop_toTopOf="@id/tv_view_room_password_label"
            app:layout_constraintBottom_toBottomOf="@+id/tv_view_room_password_label"
            app:layout_constraintStart_toEndOf="@+id/tv_view_room_password_label"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="8dp"
            android:minHeight="48dp"
            android:labelFor="@id/tv_view_room_password_label"
            android:textColorHint="@color/red33"
            android:inputType="textPassword"/>
    <Button
            android:text="@string/join"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/btn_view_room"
            style="@style/menu_button"
            app:layout_constraintTop_toBottomOf="@id/et_view_room_password"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>